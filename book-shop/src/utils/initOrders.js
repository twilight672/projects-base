/**
 * 订单初始化模块
 * 
 * 这个模块原本用于生成演示订单数据
 * 现在改为只在用户实际下单时才创建订单，不再自动生成假数据
 * 
 * 保留这个文件是为了兼容可能存在的引用
 */

/**
 * 初始化订单数据
 * 
 * 目前是空实现，订单数据完全由用户下单行为产生
 * 如果将来需要预置演示数据，可以在这里添加逻辑
 */
const initOrders = () => {
  // 不再自动创建演示订单
  // 订单数据由用户实际下单时通过 createOrder() 创建
}

export default initOrders